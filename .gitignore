# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Rust build output
src-tauri/target
src-tauri/gen

# Local build scratch (TMP redirected here — C: is tight)
.tmp/
build-latest.log

# Local handoff / research / secrets workspace (see temp/README.md);
# the workspace contract files stay tracked
temp/*
!temp/README.md
!temp/AGENTS.md

# Local-only agent config (project MCP) and code index
.agents/mcp.json
.codegraph/

# Session tooling
.claude/
pane.md

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Per-chair agent config (Cursor rules) — local like pane.md
.cursor/
